// ==============================================================================
// @file   main.cpp
// @brief  程序主入口点
// ==============================================================================

#include "app.h"

/**
 * @brief 主函数，整个应用程序的入口。
 * 
 * @param argc 命令行参数数量。
 * @param argv 命令行参数值数组。
 * @return int 程序退出码。0表示成功，非0表示失败。
 */
int main(int argc, char* argv[]) {
  // 创建并运行应用程序实例
  App app(argc, argv);
  return app.run();
}
